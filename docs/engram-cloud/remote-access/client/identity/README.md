Poné acá `client.crt`, `client.key`, `ca.crt` y `engram.token` de tu
identidad (generados con `../../server/generate-client-cert.sh` + el token
del dashboard admin, copiados por un canal seguro).

Todo lo que no sea este `README.md` está en `.gitignore` — nunca se
commitea nada de esta carpeta salvo este archivo.
