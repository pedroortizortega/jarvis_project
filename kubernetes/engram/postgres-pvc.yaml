apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: engram-postgres-data
  namespace: mcps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
