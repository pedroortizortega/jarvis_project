apiVersion: v1
kind: Service
metadata:
  name: engram-postgres
  namespace: mcps
spec:
  type: ClusterIP
  selector:
    app: engram-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
