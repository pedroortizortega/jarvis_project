# Do not commit a real .env file or Codex credentials.
# Hermes stores Codex OAuth credentials outside this template after `hermes auth`.
# Add only profile-specific, non-shared variables here when needed.
