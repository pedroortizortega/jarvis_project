apiVersion: apps/v1
kind: Deployment
metadata:
  name: llama-server-q6
  namespace: llms
spec:
  # Safe default: never run this beside vLLM or the IQ4 llama-server on one GPU.
  replicas: 0
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: llama-server-q6
  template:
    metadata:
      labels:
        app: llama-server-q6
        app.kubernetes.io/name: llama-server
        app.kubernetes.io/instance: qwen36-27b-ud-q6-k-xl
        app.kubernetes.io/part-of: local-llm-platform
    spec:
      automountServiceAccountToken: false
      runtimeClassName: nvidia
      enableServiceLinks: false
      priorityClassName: llm-priority
      terminationGracePeriodSeconds: 120
      nodeSelector:
        workload: llm
      tolerations:
        - key: dedicated
          operator: Equal
          value: llm
          effect: NoSchedule
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: llama-server
          image: ghcr.io/ggml-org/llama.cpp:server-cuda-b10156@sha256:4c0ece468721077a8bff8c114f8b0a92a8604ed310c91939fadfe37595a53b9e
          imagePullPolicy: IfNotPresent
          args:
            - --model
            - /models/Qwen3.6-27B-UD-Q6_K_XL.gguf
            - --alias
            - qwen3.6-27b-q6
            - --host
            - 0.0.0.0
            - --port
            - "8080"
            - --api-key-file
            - /run/secrets/llama/api-key
            - --n-gpu-layers
            - auto
            - --fit
            - "on"
            - --fit-target
            - "4096"
            - --ctx-size
            - "65536"
            - --parallel
            - "1"
            - --threads
            - "16"
            - --threads-batch
            - "24"
            - --flash-attn
            - auto
            - --cache-type-k
            - f16
            - --cache-type-v
            - f16
            - --no-kv-offload
            - --load-mode
            - mmap
            - --cont-batching
            - --cache-prompt
            - --cache-ram
            - "0"
            - --ctx-checkpoints
            - "4"
            - --jinja
            - --metrics
            - --no-ui
          env:
            - name: HOME
              value: /tmp
            - name: CUDA_CACHE_PATH
              value: /tmp/cuda-cache
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: "12"
              memory: 32Gi
            limits:
              cpu: "24"
              memory: 44Gi
              nvidia.com/gpu: "1"
          startupProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 240
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 4
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: models
              mountPath: /models
              readOnly: true
            - name: tmp
              mountPath: /tmp
            - name: auth
              mountPath: /run/secrets/llama
              readOnly: true
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: llama-models-q6
        - name: tmp
          emptyDir: {}
        - name: auth
          secret:
            secretName: litellm-auth
            items:
              - key: llama-api-key
                path: api-key
            defaultMode: 0440
