apiVersion: v1
kind: Service
metadata:
  name: llama-router
  namespace: llms
  labels:
    app.kubernetes.io/name: llama-router
    app.kubernetes.io/part-of: local-llm-platform
spec:
  type: ClusterIP
  selector:
    app: llama-router
  ports:
    - name: http
      port: 8080
      targetPort: http
