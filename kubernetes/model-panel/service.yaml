apiVersion: v1
kind: Service
metadata:
  name: model-panel
  namespace: llms
  labels:
    app.kubernetes.io/name: model-panel
    app.kubernetes.io/part-of: gpu-handoff-web-panel
spec:
  type: ClusterIP
  selector:
    app: model-panel
  ports:
    - name: http
      port: 8080
      targetPort: http
